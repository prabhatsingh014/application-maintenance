variables:
    # type of activity you would like to execute such as install, upgrade or rollback
    activity: install
    # package name
    package: nginx=1.18.0
    GIT_SUBMODULE_STRATEGY: recursive